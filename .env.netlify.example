# NutriWise Environment Variables for Netlify
# Copy these to your Netlify site's environment variables section

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Gemini AI Configuration  
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud / Vertex AI Configuration (for Netlify Functions)
GOOGLE_CLOUD_PROJECT_ID=your_google_cloud_project_id
GOOGLE_CREDENTIALS_BASE64=your_base64_encoded_service_account_credentials

# Optional: External Vertex AI Server (leave empty to use Netlify Functions)
# VITE_VERTEX_AI_API_URL=https://your-external-server.com

# Instructions:
# 1. Replace all "your_*_here" values with your actual credentials
# 2. For GOOGLE_CREDENTIALS_BASE64, encode your service account JSON file:
#    cat service-account-key.json | base64 -w 0
# 3. Add these to Netlify: Site Settings > Environment Variables
# 4. Redeploy your site after adding the variables
