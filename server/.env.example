# NutriWise Server - Environment Variables Template

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=your_project_id
GOOGLE_CREDENTIALS_BASE64=your_base64_encoded_service_account_json

# Server Configuration  
PORT=3001
NODE_ENV=production

# Optional: Enable debug logging (set to false in production)
DEBUG=

# CORS Configuration (optional - if you need to restrict origins)
ALLOWED_ORIGINS=https://your-netlify-app.netlify.app
